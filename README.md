# homebrew-smartcat

Homebrew tap for [smartcat](https://github.com/a0s/smartcat) — a context-aware
`cat` that renders Markdown, images, PDFs and code right in your terminal.

```sh
brew install a0s/smartcat/smartcat
```
